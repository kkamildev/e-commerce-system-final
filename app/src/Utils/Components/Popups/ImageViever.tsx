import type { FC } from "react";

type Props = {

}

const ImageViever : FC<Props> = ({}) => {
    return(
        <></>
        // <div onClick={() => {}} className={`fixed top-0 bottom-0 left-0 right-0 z-30 bg-black ${display ? "opacity-50 pointer-events-auto" : "opacity-0 pointer-events-none"} transition-all duration-700 ease-in-out`}>

        // </div>
    )
}

export default ImageViever;