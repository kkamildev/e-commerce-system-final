


export const userApi = {
    // getAll: async (): Promise<UserModel[]> => {
    //     return new Promise(resolve => {
    //         setTimeout(() => resolve(users), 300);
    //     });
    // },

    // insertOne: async (user : UserModel) => {
    //     return new Promise(resolve => {
    //         setTimeout(() => resolve(users.push(user)), 300);
    //     });
    // },
    // updateOne: async (user : UserModel) => {
    //     return new Promise(resolve => {
    //         setTimeout(() => resolve(users[users.findIndex((obj) => obj.id === user.id)] = user), 300);
    //     });
    // },
    // deleteOne: async (user : UserModel) => {
    //     users = users.filter
    // },
}